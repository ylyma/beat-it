// eslint-disable-next-line prettier/prettier, @typescript-eslint/no-unused-vars
const home = (state : any, {type, payload} : any) => {
  switch (type) {
    case 'LOGIN':
      return state;
    default:
      return state;
  }
};

export default home;
